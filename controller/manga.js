var Client = require('./dbManager');

module.exports = {
/*
    findGenre: (genre, callback) => {
        Client.connect(err => {
            if (err) {
                logger.error('findGenre() - Echec de connexion à la base de données : ' + err.message);
                throw err;
            }
            console.log("Connected!");
            var sql = "";
            Client.query(sql, (err, result) => {
                if (err) throw new Error("Ca ne fonctionne pas");
                console.log("Les documents avec le genre " + genre + "");
            });
        });
    }*/
}